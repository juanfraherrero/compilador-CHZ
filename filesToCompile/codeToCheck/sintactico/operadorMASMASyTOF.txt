{
    x = TOF(y++),                           *{Sentencia ejecutable}*
    z = x++ + TOF(y),                       *{Sentencia ejecutable}*
    y = TOF(4_s),                           *{Sentencia ejecutable}*

    IF (x++ < TOF(y))
        PRINT #x es menor que y#,
    END_IF,                                 *{Sentencia ejecutable}*
}

